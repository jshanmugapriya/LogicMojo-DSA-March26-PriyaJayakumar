#using two pointer approach
#Time complexity O(n)
#Space complexity O(1)
def valid_palindrome(arr):
  start = 0
  end = len(arr)-1
  while start <=end:
    if arr[start] == arr[end]:
      start  += 1
      end  -= 1
    else:
      return False
  return True
